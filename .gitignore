/.vscode
/.idea
/tmp-ehdsi-defs.html
/To do.txt
.DS_Store

# Generated by _preprocessMultiVersion.sh, including the build results and the
# shared publisher.jar. The content is published to the base and base-r5
# repositories by _commitToMainRepos.sh, not committed here.
/igs

# Working copies of the base and base-r5 repositories, cloned by
# _commitToMainRepos.sh
/subigs

# liquidjs, installed on demand by _preprocessMultiVersion.sh so that the
# templates can be rendered without going through npx on every file
/.liquidjs
